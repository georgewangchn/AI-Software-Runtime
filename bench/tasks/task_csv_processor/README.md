# Task: Fix CSV Data Processor

The `process_csv()` function miscalculates the total amount. Instead of accumulating all amounts, it only stores the last row's value.

Fix the total accumulation loop. Do NOT modify the test file.

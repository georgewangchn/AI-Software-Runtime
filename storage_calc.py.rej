@@ -1,30 +0,0 @@
-def storage_calc(facts: dict) -> dict:
-    """
-    Calculate required storage based on video parameters.
-    
-    This skill accepts facts in dict format with Chinese field names.
-    """
-    
-    # Extract parameters from facts using Chinese field names
-    camera_count = facts.get("监控点数量", 0)
-    storage_days = facts.get("视频存储时长", 30)
-    resolution = facts.get("分辨率", "Full HD (1080p)")
-    framerate = facts.get("帧率", 25)
-    
-    # Determine bitrate based on resolution
-    bitrate_map = {
-        "HD (720p)": 4,    # Mbps
-        "Full HD (1080p)": 8,  
-        "4K (2160p)": 25
-    }
-    bitrate = bitrate_map.get(resolution, 8)
-    
-    # Calculate total storage in TB
-    # Formula: (camera_count * bitrate * 3600 * 24 * storage_days) / (1024^3)
-    total_bytes = camera_count * bitrate * 3600 * 24 * storage_days * 1024 * 1024
-    total_tb = total_bytes / (1024**4)  # Convert to TB
-    
-    return {
-        "存储总容量TB": round(total_tb, 2),
-        "存储配置": f"{camera_count}个摄像头 × {bitrate}Mbps × {storage_days}天 = {round(total_tb, 2)}TB"
-    }
\ No newline at end of line
